<div class="container">
<h1 class="text-2xl">Editing article</h1>
<div class="text-center">
    <% if @article.image.attached? && @article.image.persisted? %>
        <%= image_tag @article.image.variant(resize_to_fit: [250, 250]) %>
        <p>Nazwa obrazka: <%= @article.image.filename.to_s %></p>
        <% else %>
        <p>Obrazek nie został załączony</p>
    <% end %>
</div>

<%= render "form", article: @article %>

<button class="bg-red font-bold p-2 rounded-full">
<%= link_to "Delete this article", article_path(@article), data: {
turbo_method: :delete,
turbo_confirm: "Are you sure you want to delete the article?"
} %>
</button>
<button class="bg-bermuda font-bold p-2 rounded-full">
<%= link_to 'Back to Articles', articles_path %>
</button>
</div>
